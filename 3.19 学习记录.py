1.可变参数：
2.参数传递的两种方式：fact(n,m)
  按位置：fact(10,5)，按名称：fact(n=10,m=5)
3.函数内部global声明局部变量，可被全局修改
4.list类型，如果局部变量未真实创建，它等同于局部变量
5.lambda返回函数名作为结果：<函数名>=lambda<参数>:<表达式>
6.turtle.write('<字>',font=('Arial',18,'normal'))